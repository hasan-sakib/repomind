# API Documentation

Not yet populated — there are no domain endpoints beyond the health check
(`GET /api/v1/health`) at this stage. Once Phase 1 (repo ingestion + chat)
lands, this directory will document each resource's endpoints, request/response
schemas, and auth requirements. The live interactive reference is always
available at `/docs` (Swagger UI) on a running non-production API instance.
