# Database Documentation

No domain models exist yet (see `backend/app/models/__init__.py`). Once Phase 1
adds the first models (users, repositories, ingestion jobs, chat sessions),
this directory will document the schema, key relationships, and indexing
decisions, alongside the Alembic migration history in
`backend/alembic/versions/`.
